resource "container-type-mount" "apache.docker.mount.data" {
  code = "apache.docker.mount.data"
  name = "apache data"
  canPersist = true
  containerPath = "/var/www"
  deployable = true
  hostPath = "/data"
  mountType = "volume"
  shortName = "data"
  sortOrder = 1
  visible = true
}