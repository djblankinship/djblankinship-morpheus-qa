resource "container-type-mount" "apache.docker.mount.logs" {
  code = "apache.docker.mount.logs"
  name = "apache logs"
  containerPath = "/var/log/apache2"
  hostPath = "/logs"
  mountType = "volume"
  shortName = "logs"
  sortOrder = 2
  visible = true
}